# MathJax Test Page

- Inline formula with `$ ... $`: $E = mc^2$

- Inline formula with `\\( ... \\)`: \\( E = mc^2 \\)

- Block formula with `$$ ... $$`:

  $$ x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a} $$

- Block formula with `\\[ ... \\]`:

  \\[ x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a} \\]
